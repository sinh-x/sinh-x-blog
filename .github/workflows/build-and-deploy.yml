
name: Build and Deploy

on:
  push:
    branches:
      - gh-pages

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Checkout private repository
        uses: actions/checkout@v2
        with:
          repository: sinh-x/personal-vault
          token: ${{ secrets.GH_PAT }}
          path: private-repo

      - name: Copy sub-directory to vault
        run: cp -R personal-vault/Areas/Stories/* ./vault/

      - name: Install dependencies
        run: npm install

      - name: Build site
        run: npx eleventy

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./_site
